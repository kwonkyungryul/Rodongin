<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="shop.mtcoding.rodongin.model.resume.ResumeCareerRepository">

    <select id="findByResumeId" resultType="shop.mtcoding.rodongin.model.resume.ResumeCareer">
        SELECT * FROM resume_career where resume_id = #{resumeId}
    </select>

    <insert id="insert">
        INSERT INTO resume_career(resume_id, career_company, career_start, career_end, created_at) VALUES
        (#{resumeCareerSaveDto.resumeId}, #{resumeCareerSaveDto.careerCompany},#{resumeCareerSaveDto.careerStart},#{resumeCareerSaveDto.careerEnd}, NOW()),
    </insert>

</mapper>