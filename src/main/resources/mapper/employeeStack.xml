<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="shop.mtcoding.rodongin.model.employee.EmployeeStackRepository">

    <insert id="insert">
         INSERT INTO employee_stack (employee_id, stack_id, stack_acquistion, created_at)
         VALUES (#{employeeId}, #{employeeStack.stackId}, #{employeeStack.stackAcquistion}, NOW())
    </insert>

    </mapper>